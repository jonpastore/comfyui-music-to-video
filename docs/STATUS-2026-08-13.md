# Status · what is built, what is specified, what is deployed

Written 2026-08-13, after the deploy. **Every line was measured, not carried** —
the commands are named so a number can be re-run. This supersedes the criterion
counts in the day-12 continuation, which were wrong for five of seven documents.

---

## 1. Deployed, and verified on the box

**Production is at `c01c977`.** Deployed from a clean detached worktree at HEAD,
both queues verified idle first, six pages 200, ComfyUI 200, xai key present.

Verified rather than assumed: `md5sum` of the deployed `app/mixer.py` and
`app/app.py` matches the worktree exactly, and `item_chains` + `master_engaged`
are both present on the box — so **the `T1-20d` fix is live**.

Before this, production had been at `f9ca597` with **27 local commits** ahead of
it, unshipped.

**`git push` is failing and this is the one thing not done.** SSH authenticates
(`ssh -T git@github.com` → *Authenticated … using publickey*), port 22 and 443
both reach GitHub, GitHub reports all systems normal, and the first attempt got
a genuine remote **Internal Server Error**. Subsequent attempts get *Permission
denied (publickey)* from git's own ssh, which never offers a key, while a direct
`ssh` does. Not the sandbox — it fails with sandboxing off too. **`origin/main`
is still at `f9ca597`; the work is deployed but not pushed.**

## 2. The fleet — confirmed live

`RENDER_BACKEND=swarm` on the box. Four backends, read from the studio's own
`/models/fleet`:

    [0] cerberus        RTX 5090 Laptop   23.42 GiB   running
    [1] gamingpc        RTX 5090          31.84 GiB   running
    [2] peaches-unraid  RTX 2080 Ti       10.58 GiB   running
    [3] ethan           RTX 5080 16 GB    idle (opportunistic)

| asked | answer |
|---|---|
| **Is Swarm up?** | **Yes.** `RENDER_BACKEND=swarm` in production, four backends registered, three running |
| **Is the pipeline work done?** | **Yes.** All eight functions from `SWARM_PIPELINE_PLAN` phases 0-4 present: `submit_swarm`, `_swarm_fetch`, `_wf_prefix`, `_attempt_plan`, `_retarget`, `_backend_vanished`, `install_input`, `_stamp` |
| **Is Unraid done?** | **Yes.** peaches-unraid is backend [2], running, 10.58 GiB visible |

**What is NOT done for those two plans: they had no acceptance criteria at all.**
That is what TRD-9 is, written today — 17 criteria over machinery that already
works, so a change to routing can be shown to have broken something.

## 3. The document set — complete

**Snapshot dated 2026-08-13. Not the grind queue.** Built-state lives in each TRD's *Status against the tree* table (refreshed 2026-08-14 against `37d18be`). Product order is `AGENTS.md`. Jarvis is not the requirements list.

Ten TRDs, three PRDs, three DDDs, one style guide. Every one reviewed by both
grok and chatgpt, all with zero fabrications.

| | criteria | PRD | DDD | UI/UX | external review |
|---|---|---|---|---|---|
| TRD-1 timeline & mixing | 34 | ✓ 1-3 | ✓ 1-3 | ✓ §1-6 | ✓ ×2 rounds |
| TRD-2 arc & storyboards | 58 | ✓ | ✓ | ✓ | ✓ ×2 |
| TRD-3 QC & remediation | 30 | ✓ | ✓ | ✓ | ✓ ×2 |
| TRD-4 character anchors | 18 | ✓ 4-7 | ✓ 4-7 | ✓ §7a | ✓ |
| TRD-5 clip render & refine | 10 | ✓ | ✓ | ✓ | ✓ |
| TRD-6 queue & lifecycle | 25 | ✓ | ✓ | ✓ | ✓ |
| TRD-7 anchor variations | 19 | ✓ | ✓ | ✓ | ✓ |
| TRD-8 audio & song editor | 15 | ✓ 8-10 | ✓ 8-10 | ✓ §7b | ✓ |
| TRD-9 fleet & operations | 17 | ✓ | ✓ | ✓ | ✓ |
| TRD-10 library & advice | 18 | ✓ | ✓ | ✓ | ✓ |

**244 criteria.** (`grep -cE "^- .T<n>-"` returns 247; TRD-4's §1a opens three
bullets with cross-references rather than declarations.)

**All ten now carry a "positive half of each one-sided criterion" table.** Seven
did not this morning.

## 4. Built vs specified, per document

Measured against the tree at `c01c977`.

| | built | notes |
|---|---|---|
| TRD-1 | automation curves, the master stage, `T1-20d`, `T1-13`/`T1-14` `mixer.peaks` | `T1-15` empty-reason, preview, audiences, the timeline itself unbuilt |
| TRD-2 | `arc.py`, the scene-count decision, `T2-41`, `T2-35` | **the clip-length chain is blocked on `T2-12a`** |
| TRD-3 | tier 1 complete (`qc.py`, `qc_service.py`, findings, `/api/qc/*`) | tier 2 unbuilt; **`approve()` raises**, so tier 3 is absent |
| TRD-4 | 6 of 18 | the save-path guard, zero-view refusal, positive body, slot naming |
| TRD-5 | 1 of 10 | only the upscaler catalogue entry. `--refine` is still a silent no-op on the default model |
| TRD-6 | 4 of 25, partially | `expect_json`, `_backend_vanished`, the migration convention, findings idempotency |
| TRD-7 | **10 of 19** | session B shipped `T7-6`, `T7-8`, `T7-9`, `T7-11`, `T7-12`, `T7-14`, `T7-15`, `T7-19` today |
| TRD-8 | the stage, not the model | `takes`, `voices`, `take_voices`, `library` — **all four absent from `db.py`** |
| TRD-9 | **the behaviour, none of the criteria** | ~1,700 lines live in production with nothing asserting it |
| TRD-10 | the modules, not the rules | `lyrics.py`, `chat.py`, `mixadvice.py`, `vision.py` exist; bulk edit unbuilt |

## 5. The suite

**241 passed, 194 `def test_`, `check_integration.py` OK** — run by both sessions
independently, and once in a clean worktree at HEAD before deploying.

That count is unchanged from this morning **and the tree is not**: this is the
first run in the sequence that includes a source change to `mixer.py`. The count
holding is the finding, not a repetition.

## 6. What is open, in priority order

1. **`git push`** — deployed but not pushed. Needs Jon; `! git push` in a session
   with an interactive keyring is the likely fix.
2. **`T2-12a`** — one rounding rule, and the whole variable-clip-length chain is
   behind it, including `T6-13a`'s one-column duration authority.
3. **The view table** (`T7-1`/`T7-3`/`T7-5`) — B's handoff is explicit that it
   must land **structurally first, in its own commit, with the four existing
   views asserted byte-identical**, because `pose` beside *"standing upright,
   arms relaxed at their sides"* is a shipped prompt contradiction and splitting
   `BACKDROP` changes every existing sheet.
4. **`approve()` raising** — tier 3 does not exist until it stops.
5. **The audio take model** — every day of use makes the missing provenance less
   recoverable.
6. **`image 2`'s role on a nude sheet** — resolved in the documents, unbuilt.

## 7. What is blocked on Jon, not on work

- The regenerated anchor sheet. `chosen` is 0 on every anchor and `T4-13` cannot
  be closed without a render.
- Whether the take model is backfilled or applied going forward.
- Whether voice cloning is wanted at all.
- Whether TRD-6's queue rewrite proceeds — **decided: yes, in full.**
