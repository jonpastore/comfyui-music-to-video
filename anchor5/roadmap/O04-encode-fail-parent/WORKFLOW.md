# O04 — VAEEncode a FAIL pose parent

**Status:** **improper.** G1 copied the FAIL.

## Purpose
Tried to keep rear cam from `lbpose` and fix the face by encoding it.

## Why it is wrong
Denoise 0.70 keeps peach face + missing arm. Parent must already
PASS the axes you are not editing.

## Do not run
Never encode `lbpose` / G1 / any pose-FAIL sheet.

## Results
`g1_s843167749`, `g1_s129080599`.
